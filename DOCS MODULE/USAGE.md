## Usage Notes

- All automation calls are dry-run by default.
- Use `dry_run=False` only with valid Instagram Graph API credentials.
- `/post` API endpoint triggers automation.
- `/simulate` starts multi-agent behavior simulation.
